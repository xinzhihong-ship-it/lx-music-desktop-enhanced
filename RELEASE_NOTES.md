# LX Music 增强版 3.2.3

本次修复两个影响正常使用的问题（下载列表看不到任务、macOS 音频转换不可用），并让重复下载、多音质下载的行为变得可预期。

## 修复

- **下载列表不显示任何任务**（3.2.2 引入）：上一版统一音质文案时，下载页渲染任务行会抛错，列表只剩表头、看不到任何任务——下载本身是正常的，文件也确实在下载目录里，只是界面上看不见。现已修复。
- **macOS 音频转换不可用**（v3.1.0 起）：随包分发的 ffmpeg / ffprobe 链接了构建机上 Homebrew 的 libxcb / libX11（绝对路径），在没有装 Homebrew 的机器上会直接加载失败，转换必然报错。现在改为只依赖系统库，并在构建脚本与打包环节各加了一道「出现系统库之外的依赖就让构建失败」的检查，防止这类「只在构建机能用」的产物再进入发布包。
- **重复下载不再静默无反应**：同一首歌、同一音质重复点「下载」时，此前会被静默丢弃。现在任务状态为已完成 / 出错时会重置进度并重新排队下载；任务正在下载 / 等待 / 暂停时会提示「列表里已有该任务」。
- **同一首歌的多个音质不再互相顶掉**：母带 / Hi-Res / 无损常常落到同一个文件名，此前会被「存在同名文件时跳过下载」直接跳过。现在检测到是同一首歌的其它音质占用该文件名时，会自动把音质写进文件名（例如 `周旋 - 王以太、艾热 AIR [SQ].flac`），多个音质可以共存；下载目录里其它软件的同名文件仍按设置跳过。

## 说明

- 升级后 3.2.2 已建立的任务记录都会正常显示；macOS 用户升级到本版后音频转换才可用（v3.1.0–3.2.2 的转换在多数机器上都是失败的）。
- 「存在同名文件时跳过下载」的语义未变：同音质重下且目标文件仍在时会跳过，但状态栏会显示结果，不再静默。

## 验证范围

- 真实应用验证：下载列表任务行渲染、重复下载的重置重下、多音质撞名改名。
- macOS 音频转换端到端验证：应用内转换队列产出的文件经 ffprobe 校验（容器 / 采样率 / 时长正确）。
- 依赖检查：内置 ffmpeg / ffprobe 只依赖 `/usr/lib` 与 `/System`；该检查能拦住此前发布包里依赖 Homebrew 的那份二进制。
- 既有单元测试（音质判定 / 播放意图 / 音频路由 / 歌单 / 基调 / 同步等）通过；ESLint、渲染进程与主进程构建通过。

---

# 历史版本摘要

## 3.2.2 — 平台音质档位对照

- 把「平台自己的音质档位」当作一等公民：设置里悬停音质选项可看各平台叫法；播放栏显示「平台档位名 (软件档位) · 引擎」；音源音质测试按各平台自己的档位表逐档判定，取不到的档位会写明原因。

| 软件档位 | 酷我 | 酷狗 | QQ | 网易 | 咪咕 |
| --- | --- | --- | --- | --- | --- |
| 128k | 高品音质 | 标准音质 | 标准音质 | 标准 | 标准音质 |
| 320k | 超品音质 | 高品音质 | HQ 高品质 | 极高 | 高清音质 |
| flac | 无损音质 | 无损音质 | SQ 无损音质 | 无损 | 无损音质 |
| hires | Hi-Res音质 | Hi-Res音质 | Hi-Res | Hi-Res / 高清臻音 | 至臻音质 |
| atmos | 杜比全景声 / DTS:X音质 | 蝰蛇全景声 / 杜比全景声 | 臻品全景声 5.1 | 杜比全景声 | 3D Audio |
| atmos_plus | 至臻音质 | — | 臻品音质 2.0 | 高清臻音 | — |
| master | 至臻母带 | 蝰蛇超清 / 蝰蛇母带 | 臻品母带 | 超清母带 | 至臻母带 |

- 修正三处档位映射：网易「高清臻音」不再被当成全景声（真正的杜比全景声此前取不到）；补上酷狗「蝰蛇母带」；咪咕改用真实存在的档位码 `ZQ24`。

## 3.2.1 — 音质测试与口径统一（含 beta 期的 VST3、基调识别）

- 音源音质测试（设置 → 基本设置 → 音乐来源）：多选音源逐档请求，给出「请求档位 → 实际档位」与规格判定；播放栏显示实际播放音质。
- 音质口径对齐 ikun：收敛为 7 档（母带 / 全景声2.0 / 全景声 / Hires / FLAC / 320K / 128K），24bit 无损统一算 Hires；旧键 `flac24bit` / `192k` 保留兼容并自动迁移。
- VST3 音频链（实验性，仅内置播放引擎）、采样率显示与音频输出设备切换。
- 歌曲基调识别与机架同步：实时调性 / Camelot 轮盘、动态转调跟踪、分段微调与永久记忆、独立置顶悬浮窗、Auto-Key 毫秒级同步。
- 修复：酷狗歌单顺序与加删、首次点击播放要点两次、QQ 扫码换票与歌单写入、平台歌单刷新分页与旧响应覆盖。

## 3.1.x 及更早

- 音频转换队列（v3.1.0）、哔哩哔哩音视频播放、听歌识曲、多平台音乐账号与歌单等能力陆续加入。

更早版本的逐条改动见仓库 Releases 页与 git 历史。
