# LX Music 增强版 2.13.0-enhanced.10

本版本重点完善播放队列、搜索扩展、识曲结果确认与列表交互体验，并进一步统一实际播放音质的展示逻辑。

## 主要更新

- 播放栏新增播放队列入口，可查看当前实际播放列表与稍后播放内容，并快速定位当前歌曲。
- 在线歌曲右键菜单新增“加入播放列表”和“下一首播放”，支持多选、队首优先与重复项整理。
- 新增可配置的扩展索引来源，支持歌曲搜索、分页、歌词、封面及自定义源播放能力。
- 扩展来源的索引地址与访问配置仅保存在本地，备份导出时会自动移除访问配置。
- 听歌识曲新增多窗口结果一致性判断、候选结果展示和手动确认，可减少短片段或复杂音频造成的误判。
- 搜索列表的音质信息与播放器实际返回音质分离展示，最终播放状态以音源真实结果为准。

## 体验优化

- 播放队列继续复用现有播放器列表、随机播放与稍后播放状态，不额外维护重复队列。
- 优化长列表滚动、尺寸变化和组件销毁时的虚拟列表刷新稳定性。
- 统一播放队列、定位和关闭按钮的视觉样式，并校准播放栏队列图标的尺寸与中心线。
- 搜索页遇到已移除或不可用的历史来源配置时，会自动回退到可用来源。
- 移除实验性平台适配及相关兼容逻辑，精简不再使用的开发模块与调试输出。

## 问题修复

- 修复在线歌曲首次加入试听列表后仍使用旧列表快照，导致首次点击可能不播放的问题。
- 修复列表刷新或菜单上下文失效时，错误歌曲对象进入临时队列并触发运行时异常的问题。
- 修复“下一首播放”重复加入同一歌曲后在稍后播放队列留下重复项的问题。
- 修复部分扩展来源缓存地址与实际返回音质不一致时，播放器音质标识显示不准确的问题。
- 修复识曲引擎结果不一致时过早确认，以及候选结果被静默丢弃的问题。

## 已验证

- 播放队列定位、下一首优先、多选加入、重复整理及随机播放链路已完成实际界面验证。
- 扩展索引来源的搜索、分页、列表转换和自定义源请求参数已完成验证。
- 识曲决策单元测试、代码检查、类型检查、主进程与渲染进程生产构建均通过。
- 发布候选文件已完成敏感凭据扫描，内部计划和账号探测脚本不包含在本次发布中。

完整代码差异：[v2.13.0-enhanced.9...v2.13.0-enhanced.10](https://github.com/xinzhihong-ship-it/lx-music-desktop-enhanced/compare/v2.13.0-enhanced.9...v2.13.0-enhanced.10)
