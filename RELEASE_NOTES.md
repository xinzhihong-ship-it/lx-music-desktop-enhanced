# LX Music 增强版 3.1.4-beta.8

本测试版新增 VST3 音效插件支持，并修复搜索联想候选无法选中（Issue #20）与播放失败恢复竞态（Issue #19）。感谢反馈者提供的测试与建议。

## 新增：VST3 音效插件（实验性）

- 播放详情控制栏新增"音效插件"面板（拼图图标入口）：插件链开关、上移/下移排序、打开插件参数界面、"全部开启/关闭"批量操作。
- 独立"添加插件"选择器：按名称/厂商/路径搜索过滤、虚拟滚动列表（上千插件不卡）、扫描结果本地缓存（仅手动刷新才重新扫描）。
- 插件 DSP 运行在宿主专用音频线程：打开插件界面、浏览预设菜单不阻塞音频处理，参数实时生效；偶发卡顿时自动短间隙/直通降级，只有插件崩溃才暂停播放。
- 每个插件独立显示延迟，歌词自动补偿（保留用户偏移）；支持带辅助总线的插件（如 Acon Digital Remix 分轨输出）。
- 宿主以独立进程运行，插件崩溃不影响应用；每个实例独立保存/恢复状态。
- 设置页 VST3 区块按设置页风格重做，保留插件目录管理等高级配置。
- 当前仅内置引擎可用，mpv/Audirvana 会在面板中给出提示。

## 修正

- 搜索联想下拉候选无法选中（Issue #20）：候选点击不再与失焦收起列表竞速，慢速点击也可选中；中文输入法组词状态下的回车、方向键不再被误当成搜索/导航。
- 播放失败策略、重试次数和自定义音源数改为纵向排列，不再互相遮挡（Issue #19）。
- 策略下拉框限制为 220px，数字输入框限制为 64px（Issue #19）。
- 修正拖动策略后保存顺序相反的问题（Issue #19）。
- 强制刷新时绕过旧换源缓存；旧请求晚返回也不会覆盖最新结果（Issue #19）。
- 切歌或主动停止后，旧歌曲的异步恢复任务不会继续重新加载（Issue #19）。

## 平台与验证

- VST3 宿主交叉编译检查通过：Windows x64、Linux x64 / arm64；CI 新增三平台构建与测试工作流。
- macOS arm64 全流程实测；Windows/Linux 运行时表现欢迎测试反馈。
- 发布包自动携带 VST3 宿主二进制（交叉编译目标除外）。
- 本版本为测试版，VST3 功能为实验性，接口与行为可能继续调整。
