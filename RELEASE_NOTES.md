# LX Music 增强版 3.1.4-beta.3

本测试版基于 beta.2，重点修复桌面端与现有 LX Music 安卓端同步 Bilibili 歌曲时的兼容问题。

## 移动端同步兼容

- 桌面端向安卓端同步时，自动跳过安卓端暂不支持的 Bilibili 歌曲及 Bilibili 来源歌单。
- 桌面端之间仍保留完整的 Bilibili 歌曲同步，不修改或伪装歌曲音源。
- 修复移动端快照缺少 Bilibili 条目时可能被误判为删除的问题。

## 支持哔哩哔哩视频播放

- 新增哔哩哔哩视频画面播放能力：播放 B 站内容时，可直接在播放详情页观看视频。
- 视频画面与现有播放控制联动，支持播放、暂停、进度跳转、音量调节、停止及播放结束状态同步。
- 支持 DASH 分离音视频流组合播放，并按需携带请求头与登录 Cookie 获取媒体资源。
- 支持在顶部搜索框粘贴哔哩哔哩地址直接播放，包括 BV 号、av 号、普通视频页、分 P 参数和 b23.tv 短链接。
- 覆盖 Windows、macOS 与 Linux（X11）视频播放路径。

## 视频播放与 MPV 稳定性

- Windows 使用原生子窗口承载视频，修复画面嵌入、窗口移动与缩放跟随、显示/隐藏同步以及双击交互问题。
- macOS 使用原生 libmpv 视频桥，并补齐内置 MPV 动态库复制、依赖路径修正与签名处理。
- Linux 使用 MPV 窗口嵌入播放；当前视频嵌入路径以 X11 为准。
- 修复视频窗口未就绪、无画面、输出格式异常以及切换页面后可见性不同步等问题。
- 改进 MPV 加载、播放、暂停、停止、进度、时长和结束事件同步，降低界面状态与实际播放状态不一致的概率。
- 完善 B 站多 CDN 候选地址、请求头和 Cookie 处理，提高登录内容与分离音视频流的播放兼容性。

## 搜索体验

- 新增“搜索历史数量”设置，可自定义保留条数。
- 搜索历史上限支持 1～1000 条，默认 15 条；修改后会自动裁剪已有记录。

## 平台账号与登录

- 重新设计“平台账号管理”弹窗，已登录账号、平台选择、登录方式和二维码状态分区更清晰。
- 改为横向双栏布局，减少弹窗内部嵌套滚动，并适配较窄或较低的窗口。
- 二维码登录新增手动刷新按钮；二维码过期、加载失败或长时间未显示时可主动重新生成。
- 修复酷狗账号头像缺失：登录后自动提取并规范化头像地址，旧账号缺少头像时也会尝试补全。
- 修复酷狗图片地址中的 `{size}` 占位符及 HTTP 地址兼容问题。
- 加固酷狗头像异步补全逻辑，避免删除账号或重新登录时被旧请求覆盖。

## 平台歌单

- 平台歌单页新增账号选择器，可在 QQ 音乐、酷狗音乐、哔哩哔哩等已登录账号之间切换。
- 左侧一次只显示当前所选账号的歌单，避免多个平台歌单全部堆叠在同一列表中。
- 刷新歌单后保留当前账号选择；切换账号时清理上一账号的歌单选择、歌曲列表、筛选词和错误状态。
- 酷狗头像会同时显示在账号管理与平台歌单页面。

## 安装包与发布

- 安装包包含视频播放所需的 MPV、FFmpeg 及 Windows 视频宿主运行时。
- 打包阶段会校验关键视频运行时，缺失时终止构建，避免生成无法播放视频的安装包。
- GitHub 测试版发布现使用本文件作为完整中文更新说明。

## 已知限制

- 本版本为测试版，建议先备份重要数据。
- Linux 视频嵌入当前要求 X11；仅使用 Wayland 的环境暂不支持该嵌入路径。
- 各音乐平台接口可能随官方策略变化；如遇二维码、歌单或播放异常，请在 GitHub Issues 反馈并附上系统版本与复现步骤。
