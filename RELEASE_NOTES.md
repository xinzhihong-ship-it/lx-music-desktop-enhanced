# LX Music 增强版 3.2.3

本次修复若干影响正常使用的问题（下载列表看不到任务、含本地文件的列表空白并卡住、本地歌曲无法播放、macOS 音频转换不可用、无损大歌的基调分段只分析开头），并让重复下载与多音质下载的行为变得可预期。

## 修复

- **下载列表不显示任何任务**（3.2.2 引入）：上一版统一音质文案时，下载页渲染任务行会抛错，列表只剩表头、看不到任何任务——下载本身是正常的，文件也确实在下载目录里，只是界面上看不见。现已修复。
- **macOS 音频转换不可用**（v3.1.0 起）：随包分发的 ffmpeg / ffprobe 链接了构建机上 Homebrew 的 libxcb / libX11（绝对路径），在没有装 Homebrew 的机器上会直接加载失败，转换必然报错。现在改为只依赖系统库，并在构建脚本与打包环节各加了一道「出现系统库之外的依赖就让构建失败」的检查，防止这类「只在构建机能用」的产物再进入发布包。
- **重复下载不再静默无反应**：同一首歌、同一音质重复点「下载」时，此前会被静默丢弃。现在任务状态为已完成 / 出错时会重置进度并重新排队下载；任务正在下载 / 等待 / 暂停时会提示「列表里已有该任务」。
- **同一首歌的多个音质不再互相顶掉**：母带 / Hi-Res / 无损常常落到同一个文件名，此前会被「存在同名文件时跳过下载」直接跳过。现在检测到是同一首歌的其它音质占用该文件名时，会自动把音质写进文件名（例如 `周旋 - 王以太、艾热 AIR [SQ].flac`），多个音质可以共存；下载目录里其它软件的同名文件仍按设置跳过。
- **含本地文件的列表整片空白、界面跟着卡住**：本地歌曲（「添加本地文件」加入的歌）没有音质字段，而列表行的音质徽标直接读取它，渲染时会抛错 —— 结果是该列表一行都显示不出来，整个界面也变得点不动（键盘仍然可用）。现在列表行改用可选链读取，本地歌曲与缺少该字段的自定义音源歌曲都能正常显示。
- **本地歌曲无法播放，基调分析也跟着失效**：本地文件的播放地址会被音源脚本做百分号编码（中文 / 空格文件名，如 `白露.mp3` 变成 `%E7%99%BD%E9%9C%B2.mp3`），播放引擎按字面路径找不到文件，直接报「音频加载出错，5 秒后切换下一首」；基调分析因为等不到可用音源也一并失败。现在本地歌曲的路径在交给播放引擎前会先解码，本地文件可以正常播放与分析了。
- **独立基调窗口「重新分析」不更新**：独立窗口只在切换歌曲时重建时间轴与段落，对同一首歌点「重新分析」后，段落列表与调性轮盘会一直停在旧结果。现在按分析结果的版本号判断，重新分析完成后段落与轮盘会立刻刷新。
- **内置基调面板点「重新分析」会整个消失、底部按钮被压折行**：面板里的「重新分析」此前会先关闭面板再分析，看起来像界面没了；底部「全局同步机架 (Auto-Tune)」与「保存并应用」在窄弹窗里会被压到折行、看着不齐。现在重新分析就地刷新面板内容，底部按钮组放不下时整行换行，文字不再被压折。
- **无损大歌「重新分析」其实只分析了开头 20 秒**：基调时间轴取音频有个 48MB 上限，而一首 4 分钟的 48kHz 无损就有 50~100MB，超过就只能分析开头 4MB（约 20 秒），算出来只有 1~2 段 —— 面板按段数决定视图，于是从「分段设置」掉回「整首统一」，分段看起来像没了（歌曲带转调时差距尤其明显）。现在改用随包 ffmpeg 把整首歌解成分析用 PCM（4 分钟约 5.8MB，实测 2 秒解完），时间轴覆盖全曲，同一素材实测由 2 段恢复为 15 段。
- **重新分析期间面板被清空、失败后回不来**：点「重新分析」会先把当前结果清掉，分段列表在分析期间整块消失；分析失败（网络抖动等）时这个空状态会一直留着。现在分析期间保留当前分段（顶部提示「正在按当前音源重新分析」），新结果出来整体替换，失败则保留原结果。
- **用户自己存的分段基调打开面板显示成「整首统一」**：手动保存过多段基调的歌，再打开面板会被当成单值显示，看不到分段。现在会直接进入「分段设置」。
- **面板里点过「分段设置 / 整首统一」之后就再也看不到分析结果**：面板有个「用户动过编辑器就不再接受分析结果」的保护，但切换模式也被算成"动过"——点一下「分段设置」再等分析完成，当前调、分段时间与调都不会填进来（音名网格也不再跟着播放走），看起来像功能没做；切歌时这个标记还不会重置，新歌分析完了面板仍停在上一首的调与分段上。现在只有真正改了调式/分段才算"动过"，切歌会整体重置，分析结果照常实时填入并跟播。
- **独立悬浮窗点「重新分析」像没反应**：一是提示条只在「还没有任何结果」时才显示，而重新分析期间会保留旧结果，于是完全没有"正在分析"的反馈；二是如果这首歌是你手动保存过基调的，重新分析按设计不会覆盖它，但界面什么也不说。现在重新分析会立刻提示「正在重新分析…」，已手动记忆的歌会明确提示「重新分析不会覆盖它，想重新识别请先点恢复自动识别」（内置面板同样提示）。
- **独立悬浮窗播放中改不了分段时间**：窗口每 500ms 同步一次播放进度，每次都会重建整张段落列表，导致双击改时间时的输入框被重建、输入内容丢失（暂停时不出现）。现在编辑期间不再重建列表。
- **独立悬浮窗切换模式后段落不再跟播**：与内置面板同一个坑（切模式被当成"动过编辑器"），现在切模式只记住你的选择，不影响跟播与数据刷新。
- **独立悬浮窗的即时试听不受「全局同步机架」开关约束**：以前关着开关点音名也照样推给 Auto-Tune 插件，现在与内置面板一致（关就不推）。另外，用户自己保存的多段基调在悬浮窗里不再被显示成「整首统一」。
- **「记住这首歌的基调」以前是个没接线的勾选框**：内置面板里它只负责显示（保存逻辑从来没读它），而且被放在「整首统一」里面——切到「分段设置」就看不见；独立悬浮窗连这个勾选框都没有。现在两个界面都把它放到吸底条、与「全局同步机架」并排，两种模式下都可见，并且真正生效：勾选 = 写入本地记忆（下次播放直接采用）；取消勾选 = 只在本次播放生效，不写记忆（下次播放仍走曲库 / 重新分析），来源标签会显示「仅本次」。
- **macOS 上静音 / 取消静音与音量滑块要等几秒才生效**：MPV 引擎在 macOS 上被固定到了 `avfoundation` 音频驱动，而它是「提前排队」式的输出——会把约 2 秒的音频先排进系统渲染器，音量与静音又是在数据入队之前施加的，于是要等队列放完才听得见（暂停、拖进度、切歌不受影响，因为会清空队列）。现在改为优先使用 mpv 在 macOS 的默认驱动 `coreaudio`（缓冲 0.2 秒，静音与音量都跟手）；对 coreaudio 打不开的设备（实测部分 USB 声卡、聚合设备会报 channel layout 错误）会自动回退到 `avfoundation` 并记住该设备，**不会出现「选了设备没声」**。仍走 `avfoundation` 的设备上，静音还会额外下发给驱动自身的静音通道（支持则即时生效，不支持时行为与原来完全一致）。

## 说明

- 升级后 3.2.2 已建立的任务记录都会正常显示；macOS 用户升级到本版后音频转换才可用（v3.1.0–3.2.2 的转换在多数机器上都是失败的）。
- 「存在同名文件时跳过下载」的语义未变：同音质重下且目标文件仍在时会跳过，但状态栏会显示结果，不再静默。
- macOS 上 mpv 的输出驱动会改用 `coreaudio`（mpv 默认驱动，声音更跟手）；某台设备若 `coreaudio` 打不开，会自动回到 `avfoundation` 并把该设备记在配置里（`player.mpv.aoFallbackDevices`），之后不再重复尝试。B 站视频播放不受此影响。

## 验证范围

- 真实应用验证：下载列表任务行渲染、重复下载的重置重下、多音质撞名改名、含本地文件的列表渲染与切换。
- 本地歌曲播放验证：日志确认播放引擎以解码后的路径成功打开文件（mp3 / 44.1 kHz / 320 kbps）并正常推进，不再出现 playback error。
- 独立基调窗口验证：注入同一首歌的新分析结果后，段落列表由 1 段重建为 3 段、段落区随之显示。
- 内置基调面板验证：点「重新分析」后面板保持打开并就地刷新；底栏的「全局同步机架 (Auto-Tune)」「重新分析」「保存并应用」均为单行渲染。
- 基调全曲时间轴验证：对 53.8MB 无损点「重新分析」，面板在分析期间保持打开并提示「正在按当前音源重新分析」，约 6 秒后自动切到「分段设置」并给出 5 段（含 32 / 48 / 64 / 72 秒处的转调点，旧实现只看得到前 20 秒）；同一素材用内置 ffmpeg 整曲解码独立复算，得到的时间轴完全一致。
- 基调面板跟播验证：面板开着先点「分段设置」再切歌，面板随即清空并填入新歌结果（早期结果 1=E 大调 → 完整结果 `Ebm 小调 / 12 段`），播放跨过转调点时面板徽标与音名网格高亮实时同步（`Ebm → 1=B → 1=E`）。
- macOS 音频转换端到端验证：应用内转换队列产出的文件经 ffprobe 校验（容器 / 采样率 / 时长正确）。
- macOS 音频驱动验证（真实应用 + 独立 mpv 探测）：每台设备逐个用静音素材试起 coreaudio —— 内置扬声器 / Monster×4 / 直播设备 / Loopback 伴奏设备均正常，Duet 3 与聚合设备报 channel layout 错误（因此需要回退）。应用内实测：默认设备经 coreaudio 正常出流（格式与原来一致 44100/s16/stereo）；切到 Duet 3 时 mpv 自动回退到 avfoundation、音频正常推进，并写入回退名单；切回后恢复 coreaudio。静音链路实测：coreaudio 下 mpv 音量 100→0→100；avfoundation 下同时下发驱动级静音（`mute=true` + `ao-volume=0`）且取消后完整还原。
- 依赖检查：内置 ffmpeg / ffprobe 只依赖 `/usr/lib` 与 `/System`；该检查已在 CI 上拦下 x64 版链到构建机 Homebrew liblzma 的产物。
- 既有单元测试（音质判定 / 播放意图 / 音频路由 / 歌单 / 基调 / 同步等）通过；ESLint、渲染进程与主进程构建通过。

---

# 历史版本摘要

## 3.2.2 — 平台音质档位对照

- 把「平台自己的音质档位」当作一等公民：设置里悬停音质选项可看各平台叫法；播放栏显示「平台档位名 (软件档位) · 引擎」；音源音质测试按各平台自己的档位表逐档判定，取不到的档位会写明原因。

| 软件档位 | 酷我 | 酷狗 | QQ | 网易 | 咪咕 |
| --- | --- | --- | --- | --- | --- |
| 128k | 高品音质 | 标准音质 | 标准音质 | 标准 | 标准音质 |
| 320k | 超品音质 | 高品音质 | HQ 高品质 | 极高 | 高清音质 |
| flac | 无损音质 | 无损音质 | SQ 无损音质 | 无损 | 无损音质 |
| hires | Hi-Res音质 | Hi-Res音质 | Hi-Res | Hi-Res / 高清臻音 | 至臻音质 |
| atmos | 杜比全景声 / DTS:X音质 | 蝰蛇全景声 / 杜比全景声 | 臻品全景声 5.1 | 杜比全景声 | 3D Audio |
| atmos_plus | 至臻音质 | — | 臻品音质 2.0 | 高清臻音 | — |
| master | 至臻母带 | 蝰蛇超清 / 蝰蛇母带 | 臻品母带 | 超清母带 | 至臻母带 |

- 修正三处档位映射：网易「高清臻音」不再被当成全景声（真正的杜比全景声此前取不到）；补上酷狗「蝰蛇母带」；咪咕改用真实存在的档位码 `ZQ24`。

## 3.2.1 — 音质测试与口径统一（含 beta 期的 VST3、基调识别）

- 音源音质测试（设置 → 基本设置 → 音乐来源）：多选音源逐档请求，给出「请求档位 → 实际档位」与规格判定；播放栏显示实际播放音质。
- 音质口径对齐 ikun：收敛为 7 档（母带 / 全景声2.0 / 全景声 / Hires / FLAC / 320K / 128K），24bit 无损统一算 Hires；旧键 `flac24bit` / `192k` 保留兼容并自动迁移。
- VST3 音频链（实验性，仅内置播放引擎）、采样率显示与音频输出设备切换。
- 歌曲基调识别与机架同步：实时调性 / Camelot 轮盘、动态转调跟踪、分段微调与永久记忆、独立置顶悬浮窗、Auto-Key 毫秒级同步。
- 修复：酷狗歌单顺序与加删、首次点击播放要点两次、QQ 扫码换票与歌单写入、平台歌单刷新分页与旧响应覆盖。

## 3.1.x 及更早

- 音频转换队列（v3.1.0）、哔哩哔哩音视频播放、听歌识曲、多平台音乐账号与歌单等能力陆续加入。

更早版本的逐条改动见仓库 Releases 页与 git 历史。
