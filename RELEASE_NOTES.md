# LX Music 增强版 2.13.0-enhanced.11-beta.12（测试版）

本测试版完善多平台相似歌曲聚合推荐。测试版可能存在不稳定，建议先备份数据。

## beta.12 优化

- QQ、网易云、酷狗等平台分别参与相似歌曲推荐。
- 相同歌曲跨平台合并显示，避免同一首歌重复出现。
- 合并各平台音质、专辑和时长信息，并保留平台切换能力。
- 修复 QQ 音乐相似推荐使用错误歌曲 ID 导致无法参与的问题。

## beta.11 内容

## beta.11 优化

- 优化播放设置页面布局、间距和字体层级。
- MPV 可执行文件路径输入框始终可用，支持填写自定义路径，留空时自动查找。
- 恢复 MPV 设置帮助图标的悬停提示。

## beta.10 内容

## beta.10 修复与优化

- 增加内置 mpv IPC 启动等待时间，并在启动失败时清理残留进程，减少首次播放提示 `mpv IPC connect timeout` 的问题。
- 播放详情页底部控制栏增加播放队列入口。
- 播放详情页歌曲信息下显示当前音质与播放引擎，例如 `flac · MPV`。

## beta.9 修复

- 修复 Win7 专用安装包与绿色包未正确解析部分 JSON 响应，导致网易云二维码登录及其他平台功能无法使用的问题。

## beta.8 新增

- 设置 → 数据同步新增 WebDAV：支持测试连接、手动/自动同步全部歌单，以及上传、下载设置和自定义音源。
- 自动同步在应用启动、窗口重新获得焦点和歌单变化后触发；通过同步快照判断上传、下载或冲突，冲突时由用户选择保留本地或云端。
- 使用 ETag 条件写入与串行锁减少并发覆盖风险；下载覆盖前自动备份本地数据到数据目录的 `webdav-backups` 文件夹，最多保留 10 份。
- WebDAV 密码只保存在本机，不上传到云端，也不包含在普通设置导出中；建议使用 HTTPS 服务。

## beta.7 优化

- 搜索、歌单、听歌识曲、下载、设置等界面的可见滚动条统一加宽到 10px，并同步修正列表表头对齐。
- 播放详情页左下方保留与进入前位置对齐的歌曲封面，点击封面即可返回普通界面。

## beta.6 修复

- 本地收藏、平台音乐和当前播放队列新增可见的即时过滤栏，支持按歌曲名、歌手、专辑匹配；`Ctrl/Cmd+F` 定位与高亮继续保留。
- 音乐列表滚动条加宽到约 10px，并同步修正表头留白，覆盖本地列表、在线列表、平台音乐和播放队列。
- 修复“导出设置”和“导出所有数据”可能触发 `DataCloneError` 或静默失败的问题；导出会等待文件写入并显示成功或失败。
- 修复备份导入后列表未立即恢复的问题；导入设置不再重置已接受的许可协议，导入成功或失败均有明确提示。
- 自定义源会记录在线链接或本地来源；在线源可复制链接，本地及历史源可导出原始 JS。
- 我的列表、排行榜和歌单统一显示实际最高音质标签；QQ、网易、酷狗补齐 `Master`、`Atmos` 等详情，酷我、咪咕按接口真实返回显示 `24bit/SQ/HQ`。
- Windows 任务栏收藏、上一首、播放/暂停、下一首回调已增加单元测试；实际系统按钮是否恢复仍需使用本测试版 Windows 安装包验证。

## beta.6 范围说明

- Google Drive 云备份不在本版范围内。
- 历史收藏列表需要在“列表更新管理”中刷新一次，才能写入新增的高阶音质元数据。

## beta.5 修复

- 修复高音质偏好下咪咕播放“加载失败”的问题：兜底音质只保留 128k，避免换源候选被全部过滤；换源后仍按你的音质偏好播放。
- GitCode 索引地址为空时自动回退到默认索引；搜索加载失败时显示真实原因（如索引 404、平台风控），便于定位。
- 修复开发模式下的启动崩溃与热更新后界面卡死问题（仅影响开发环境）。

## beta.4 修复

- 修复 GitCode 在配置非默认索引时搜索与播放不自洽的问题：播放优先走自定义源解析，解析失败时回退到本地索引自解析（直接使用记录中的下载地址），用户配置任意 GitCode 音乐索引均可正常播放；自定义源未声明 GitCode 时菜单操作不再被禁用。

## beta.3 修复

- 修复咪咕在搜索页右键菜单播放、稍后播放、下载等操作被禁用的问题：自定义源未声明咪咕时，不再整体禁用操作，播放继续走落雪现有换源链路（与双击播放行为一致）。

## beta.2 修复

- 修复哔哩哔哩在搜索页无法播放的问题：B 站为自解析平台（不依赖自定义源），但播放入口被“音源支持”判断拦截，现已将其纳入受支持来源，搜索页可直接播放、行内播放按钮与右键菜单播放项恢复正常。
- 修复播放音质选择因来源支持列表缺失而始终回退到 128k 的问题，B 站歌曲现按实测最高音质（通常 192k）播放。
- 开发模式进一步过滤 Vue HMR 良性运行时错误遮罩（捕获阶段阻断，不影响其他错误提示）。

## 主要更新

- 新增哔哩哔哩搜索来源：WBI 签名搜索、合集/多 P 自动展开，BV、CID、分 P 信息完整保留。
- 播放链路自包含：按视频实际音频流直接取流，内置播放器与 MPV 均可正常播放，失败时继续走落雪原有换源逻辑。
- 搜索列表音质徽标改为整页实测：显示当前账号实际可播放的最高音质（通常为 192K），徽标体系与现有 HQ/SQ/Hi-Res/杜比一致。
- 触发平台风控时自动停止探测，未探测条目标记“风控”标识。
- 播放队列支持单曲移除与全部清空（带确认提示），继续复用现有播放器列表状态。

## 平台账号

- 账号管理新增哔哩哔哩：支持扫码登录与 Cookie（SESSDATA）登录。
- B 站收藏夹并入平台歌单：可浏览、添加、移除收藏内容。
- 登录后播放与搜索自动携带登录态：192k 以上音频流（杜比全景声、Hi-Res 无损）需要视频提供高阶音轨且账号具备大会员权限，实测可用即会自动显示对应徽标。
- 相似歌曲推荐新增哔哩哔哩来源（相关视频推荐）。

## 问题修复与体验优化

- 修复 B 站图床防盗链导致的平台头像无法显示。
- 修复设置页 192k 音质选项显示原始键名的问题，补充“优质 192K”文案。
- 新增 192K 音质徽标档位（GitCode 来源同样受益）。
- 优化开发模式热更新体验，过滤 Vue HMR 引起的良性运行时错误遮罩（仅开发环境，不影响生产）。

## 感谢

本版本的实现参考了以下开源项目，在此表示感谢：

- [Miao-moe/lx-Miao-moe-music-desktop](https://github.com/Miao-moe/lx-Miao-moe-music-desktop)：哔哩哔哩扩展音源的移植思路。
- [maotoumao/MusicFreePlugins](https://github.com/maotoumao/MusicFreePlugins)：哔哩哔哩插件的接口与选流参考。
- [SocialSisterYi/bilibili-API-collect](https://github.com/SocialSisterYi/bilibili-API-collect)：哔哩哔哩接口文档（WBI 签名、DASH 音频流标识）。
- [ikunshare/ikun-music-desktop](https://github.com/ikunshare/ikun-music-desktop)：GitCode 扩展索引来源的实现参考。
- [WalnutBai/lx-lxwalnut-music-mobile](https://github.com/WalnutBai/lx-lxwalnut-music-mobile)：WebDAV 同步交互与数据组织方式的参考。

## 测试说明

- 哔哩哔哩搜索、播放、歌词（字幕转换与精确匹配）、收藏夹、扫码登录均已完成实际界面验证。
- 播放队列单曲移除、全部清空、定位当前歌曲已通过实际操作验证。
- WebDAV 路径、哈希与同步决策单元测试通过，并已使用本地 WebDAV 服务验证连接、目录创建、歌单/设置/音源上传、歌单下载及覆盖前备份。
- 识曲决策单元测试、代码检查、类型检查、主进程与渲染进程生产构建均通过。
- 已知边界：192k 以上音频流取决于视频是否提供高阶音轨及账号大会员权限；未提供时显示实测最高档位 192K。

完整代码差异：[v2.13.0-enhanced.11-beta.10...v2.13.0-enhanced.11-beta.11](https://github.com/xinzhihong-ship-it/lx-music-desktop-enhanced/compare/v2.13.0-enhanced.11-beta.10...v2.13.0-enhanced.11-beta.11)
