# LX Music 增强版 3.2.2

上一版把档位名统一成了 ikun 口径，但各个平台对同一档位的叫法与真实规格并不相同：同样是「Hires」，酷狗叫 Hi-Res、网易叫高清臻音；同样是「无损」，QQ 交付的文件常常是 24bit，而酷狗是 16bit；网易真正的杜比全景声与它的「高清臻音」也常被混为一谈。结果是同一个音频文件，在音质测试页和播放栏会落到不同档位。

本次把「平台自己的音质档位」引入为一等公民：平台怎么叫、对应我们软件里的哪一档、该是什么规格，都能直接看到；音质测试也改按各平台自己的标准来判定。

## 新增

- **设置里能看到各平台的叫法**：「设置 → 播放设置 → 优先播放的音质」，鼠标停在任一声质选项上，会列出该档位在各平台的名称。例如 Hires 档显示：酷我「Hi-Res音质」、酷狗「Hi-Res音质」、QQ「Hi-Res」、网易「Hi-Res / 高清臻音」、咪咕「至臻音质」。
- **播放栏按播放平台显示档位名**：标签改为「平台档位名 (软件档位) · 引擎」，例如播放酷狗的歌会显示「无损音质 (SQ) · MPV」；鼠标悬停可看到该平台档位的完整说明与对应软件档位。
- **音源音质测试按平台标准判定**：改为以平台自己的档位表为基准，平台有哪几档就测哪几档，结果给出「平台档位名 → 实测落点 → 对应软件档位」。当前音源无法提供的档位（如酷狗的蝰蛇母带、QQ 的流畅 48k、咪咕的全曲试听）会列为不可测并写明原因，不再静默跳过。

## 平台档位对照

档位名称取自各平台官方客户端与官方接口文档，供参考：

| 软件档位 | 酷我 | 酷狗 | QQ | 网易 | 咪咕 |
| --- | --- | --- | --- | --- | --- |
| 128k | 高品音质 | 标准音质 | 标准音质 | 标准 | 标准音质 |
| 320k | 超品音质 | 高品音质 | HQ 高品质 | 极高 | 高清音质 |
| flac | 无损音质 | 无损音质 | SQ 无损音质 | 无损 | 无损音质 |
| hires | Hi-Res音质 | Hi-Res音质 | Hi-Res | Hi-Res / 高清臻音 | 至臻音质 |
| atmos | 杜比全景声 / DTS:X音质 | 蝰蛇全景声 / 杜比全景声 | 臻品全景声 5.1 | 杜比全景声 | 3D Audio |
| atmos_plus | 至臻音质 | — | 臻品音质 2.0 | 高清臻音 | — |
| master | 至臻母带 | 蝰蛇超清 / 蝰蛇母带 | 臻品母带 | 超清母带 | 至臻母带 |

各平台还有自己独有的档位（酷我的「流畅音质」、酷狗的「蝰蛇超清」、QQ 的「流畅音质 / 黑胶唱片 / TQ 标准音质」、网易的「较高」、咪咕的「全曲试听」），软件里没有对应档，测试时会列为不可测。

## 修复

- **网易**：`jyeffect`（高清臻音，实测 96kHz / 24bit 无损）此前被当成「臻品全景声」，而真正的杜比全景声 `dolby` 反而永远取不到；`higher`（192kbps）此前被当成 128K。
- **酷狗**：补上官方定义的「蝰蛇母带」（`viper_tape`）与杜比全景声。该档位多数曲目没有、接口会回落成 320K，现在会在测试结果里明确标出这一点。
- **咪咕**：此前使用的档位码 `ZQ` 并不存在（实际是 `ZQ24` / `ZQ32`），导致该档位永远取不到。
- **设置页**：修复音质选项悬停时，鼠标停在文字上不弹出提示的问题。

## 说明

- QQ 的 `size_new[1]` / `[2]`（对应「臻品音质 2.0」与「臻品全景声 5.1」）两份参考文档说法相反，当前按 QQMusicApi 的模型文档落地，待后续实测校正。
- 音质测试结果只代表当前歌曲样本与当前时刻，不代表音源的永久能力。

## 验证范围

- 新增平台音质字典、按平台判定、测试计划、档位文案四组单元测试；原有音质判定规则测试零修改通过。
- ESLint、渲染进程 / 主进程构建通过。
- 在真实音源上实机验证：设置页悬停对照表、播放栏平台档位名、酷狗与 QQ 的音源音质测试逐档落点。

---

# LX Music 增强版 3.2.1

本次正式版汇总了 3.2.1 系列测试版的改动，重点是音源音质测试与音质判定口径。

## 新增

- 「设置 → 基本设置 → 音乐来源 → 音源音质测试」：可多选已导入音源，按平台逐档请求并给出判定。关键词支持「歌名」或「歌名 歌手」（歌手可省略），支持一键填充到全部平台与一键清空；结果按平台显示实际最高音质、平台状态（`[OK]` / `[WARN]` / `[FAIL]`）和逐档「请求档位 → 实际档位」，并列出探测到的格式、采样率、位深与文件大小。
- 播放栏显示实际播放音质：播放时探测音频流，按设置中的档位名称显示真实规格（如 臻品母带 / Hires 无损24-Bit / 320K 高音）。探测只用于显示，不影响播放、换源、下载与地址缓存。

## 音质口径（对齐 ikun-music-desktop）

- 档位统一为 master / atmos_plus / atmos / hires / flac / 320k / 128k；24bit 无损一律算 Hires，母带只要求「无损且 24bit」，不再额外校验采样率。
- 有损只分 128K 普音与 320K 高音，中间码率不猜档位：有损请求沿用所选档位，无损请求按 128K 普音提示降质。
- 各平台可用档位按 ikun 收敛；酷我、酷狗、网易、咪咕、QQ 采集到的 24bit 档位键统一为 hires，修掉同平台不同入口档位不一致的问题。
- 旧键 `flac24bit` / `192k` 保留兼容：旧歌单、旧音源脚本与 bili 等平台元数据仍可正常使用；播放设置收敛为 7 档，旧设置值自动迁移。

## 修复

- 酷狗歌单：使用真实歌单条目 ID，修复歌曲顺序与添加/删除确认，写入后回读核对。
- 修复首次点击播放需要点击两次才出声的问题。
- QQ 音乐：修复扫码换票失败与歌单写入失败。
- 平台歌单刷新：修复分页与重复页导致的错误停止、循环，各账号独立加载，快速切换不再被旧响应覆盖。

## 验证范围

- 判定规则与平台档位集合单元测试全部通过；采样率探测、播放意图、音频路由、WebDAV 同步、酷狗歌单等回归测试通过；ESLint、主进程 / 渲染进程 / 歌词进程构建通过。
- GitHub Actions 多平台构建与 VST3 音频检查通过（macOS arm64 / x64、Windows、Windows 7、Linux）。
- 音质测试结果只代表当前歌曲样本与当前时刻，不代表音源的永久能力；不同平台、不同音源的实机表现仍建议自行验证。

---

# LX Music 增强版 3.2.1-beta.9

这是面向跨平台用户的测试版，把音质判定与显示口径对齐 ikun-music-desktop。

## 本次调整

- 判定口径对齐 ikun：24bit 无损一律算 Hires（不再有「24bit FLAC」这一档），母带只要「无损且 24bit」即认可，不再额外要求采样率 ≥88.2kHz。
- 有损档位只分 128K 普音 / 320K 高音：≤180kbps 判 128K，≥280kbps 判 320K；中间码率不猜档位，有损请求时沿用请求档位，无损请求时按 128K 普音提示降质。
- 平台可用档位按 ikun 收敛：酷我/QQ 有全景声 2.0，酷狗/网易没有；咪咕最高到 Hires。
- 判定不再产出 `flac24bit` / `192k`；这两个键保留用于兼容旧歌单与旧音源脚本，新旧键互为别名，旧脚本的 24bit 档位不会失效。
- 文案统一成 ikun 口径：128K 普音 / 320K 高音 / FLAC 无损 / Hires 无损24-Bit / 臻品音质 / 臻品音质2.0 / 臻品母带；列表标签 Master / Atmos2.0 / Atmos / Hires / SQ / HQ。
- 播放设置的可选音质收敛为 7 档（母带 / 全景声2.0 / 全景声 / Hires / FLAC / 320K / 128K），旧设置值 `flac24bit` → `hires`、`192k` → `320K` 自动迁移。
- 音源采集端的 24bit 档位键统一改为 `hires`（酷我、酷狗、网易、咪咕、QQ 与账号面板），修掉此前「酷狗搜索显示 hires、其它入口显示 flac24bit」的自相矛盾。
- 音质测试弹窗、下载弹窗与下载列表的档位文案同步成同一套。
- GitCode 音源保留索引中可能声明的母带 / 全景声 / Hi-Res 档位，只去掉 192k 与 flac24bit，避免自定义索引里的高规格档位失效。

## 验证范围

- 判定规则单元测试 7 项（24bit→Hires、母带不校验采样率、16bit 降质、有损档位边界、不再产出旧键、探测失败沿用请求档位、码率估算）与平台档位集合断言 2 项全部通过；采样率探测、播放意图等回归测试同步通过。
- ESLint、主进程/渲染进程/歌词进程构建通过。
- 播放栏与音质测试在真实音源上的显示效果、Windows/Linux 实机仍需继续验证，反馈请附音源名称、平台与脱敏日志。

---

# LX Music 增强版 3.2.1-beta.8

这是面向跨平台用户的测试版，修复 beta.7 中“实际音质显示偏低”和“音质探测影响播放逻辑”的问题。

## 本次修复

- 修复母带、Hi-Res 等高规格歌曲被显示成更低档位的问题：24bit 高采样率无损现在保留请求档位名称（母带 Master / Hires / 24bit FLAC），不再降级成普通无损或 Hires。
- 音质探测结果只用于显示，不再写回播放音质状态；播放失败后的“降低音质重试”、换源、播放地址缓存等逻辑恢复为 beta.6 的行为，播放和下载不受影响。
- 只有拿到确凿规格证据才判定降质：请求无损却返回有损编码、请求 320K 却返回低码率、请求 24bit 却返回 16bit 无损。证据不足（M4A 容器、探测失败）时沿用请求档位，不再显示“音质待确认”。
- 音源音质测试结果新增位深与档位结论，例如 `FLAC · 192kHz · 24bit · 124.96MB · 规格符合：母带`，与播放栏显示使用同一套换算规则，不再出现“请求母带却像被标成 FLAC”的矛盾结论。
- 新增音质换算单元测试 10 项，覆盖母带、Hi-Res、24bit、16bit、320K、128K、WAV/APE 请求、M4A 容器、探测失败和码率估算场景。
- 修复换歌或重新取址期间的档位残留：切换歌曲、换源、降质重试时先清空探测结果，地址请求失败也不会一直显示上一首的档位。

## 验证范围

- `node --test tests/actualQuality.test.cjs tests/sourceSampleRate.test.cjs tests/playbackIntent.test.cjs` 全部通过（36 项通过，3 项因本机缺少可用 ffmpeg 跳过）。
- ESLint、主进程/渲染进程/歌词进程构建通过。
- 已修正播放意图回归测试桩数据；Windows、Linux 与不同第三方音源仍需实机反馈，反馈请附音源名称、平台和脱敏日志。

---

# LX Music 增强版 3.2.1-beta.7

这是面向跨平台用户的测试版，新增多音源音质测试，并让播放栏优先显示已探测到的实际音频规格。

## 本次新增与调整

- 在「设置 → 基本设置 → 音乐来源」增加“音源音质测试”入口，可选择多个已导入音源，对固定歌曲样本逐平台、逐档位测试，并保留诊断结果。
- 测试任务使用独立实例、请求队列和任务 ID，不切换当前播放音源，不使用播放地址缓存；停止、关闭和重新开始时不会把旧响应写入新任务。
- 播放栏在线歌曲在探测完成前显示“音质待确认”，探测到实际规格后按设置中的名称显示 `128K`、`192K`、`320K`、`FLAC`、`24bit FLAC` 或 `Hires` 等结果。
- 对 Master、Atmos、M4A 等仅凭有限音频头无法确认的情况保留“音质待确认”，避免把请求档位当成实际播放规格。
- 音频探测使用有上限的 Range 读取和 `music-metadata` 解析；播放、下载、音源切换及既有播放引擎接口保持独立。

## 验证范围

- ESLint、主进程构建、渲染进程构建通过。
- 音频转换测试 3/3 通过，音乐识别测试 5/5 通过。
- 音源探测测试 6 项通过；3 项编码器测试因本机 bundled ffmpeg 动态库不可用而跳过。
- macOS arm64 本机已验证测试入口、播放栏待确认状态及实际 `24bit FLAC` 标签；Windows、Linux 和不同第三方音源仍需用户在实际环境继续验证。

---

# LX Music 增强版 3.2.1-beta.6

这是面向跨平台用户的测试版，重点修复酷狗歌单加载、歌曲顺序以及添加和删除确认问题，同时保留 QQ 音乐歌单修复和平台歌单刷新改进。

## 本次修复

### 1. 酷狗歌单加载与加删

- 酷狗歌单详情使用歌曲 Hash 匹配并统一大小写，保留歌曲顺序和真实歌单条目 `fileid`。
- 读取 `sort/list_sort` 显示最新添加的歌曲在前，避免酷狗接口的旧到新存储顺序让新歌显示在底部。
- 删除只使用已确认的歌单条目 `fileid`，缺失时重新读取歌单；不会把歌曲 ID 或 `audio_id` 当成删除 ID。
- 严格检查 HTTP、外层和业务返回结果；请求超时或结果不明确时先回读核对，不重复提交。
- 加删成功后回读目标歌单，确认云端结果后再提示成功并刷新列表。

### 2. 平台歌单刷新体验

- 修复缺少总数、重复分页造成的错误停止和循环，保留歌曲标识映射与显示顺序。
- 各账号独立加载，慢账号不会阻塞其他账号；刷新失败时保留旧列表并显示提示。
- 快速切换和连续刷新使用请求序号隔离旧响应；平台歌单选择会记住上次账号和歌单。

## 验证范围

- 酷狗歌单测试：13 项通过。
- Issue #21 回归测试：17 项通过。
- ESLint、应用构建和 `git diff --check` 通过。
- 本版本仍建议使用测试歌单分别验证酷狗添加、删除以及 QQ/网易云歌单操作；不同登录方式和系统的云端实测结果请在 [Issues](https://github.com/xinzhihong-ship-it/lx-music-desktop-enhanced/issues) 反馈。

## 建议与交流

仓库 [Discussions](https://github.com/xinzhihong-ship-it/lx-music-desktop-enhanced/discussions/18) 中已有微信群二维码，欢迎喜欢提建议、交流使用体验的朋友加入。请勿在群里发送账号 Cookie、token 等敏感信息；反馈问题时请附版本、系统和复现步骤。

---

# LX Music 增强版 3.2.1-beta.5

这是面向跨平台用户的测试版，重点修复暂停后后台播放状态和首次点击播放需要重复点击的问题。

## 本次修复

- 修复音频在 `canplay` 阶段尚未同步到界面播放状态时被误暂停，首次点击播放即可正常启动。
- 保留用户明确的播放/暂停意图；用户主动暂停后，迟到的加载、缓冲或错误回调不会重新启动音乐。
- 增加播放意图与 `canplay` 竞态回归测试，覆盖后台暂停、延迟加载和恢复请求失效场景。

## 验证范围

- macOS arm64 本机 Electron 窗口：首击播放、暂停、切换到后台等待后恢复，播放状态符合预期。
- `node tests/playbackIntent.test.cjs`：15 项通过；`npm run lint -- --no-fix`：通过。
- Windows/Linux 及不同音源服务的长时间播放仍需要用户在实际环境中继续验证。

---

# LX Music 增强版 3.2.1-beta.4

这是面向跨平台用户的测试版，重点修复 QQ 音乐扫码登录、歌单加删失败和平台歌单刷新慢的问题。请通过 GitHub Release 手动下载测试，不承诺 beta 自动更新。

## 本次修复

### 1. QQ 音乐扫码登录与歌单写入
- 修复 QQ 音乐二维码显示后扫码换票失败、界面误报“HTTP 200”的问题：改用当前 H5 登录网关签名请求和有效的客户端参数，并严格检查外层、子请求和业务返回码。
- 登录结果保留明确的登录类型；换票失败不会把临时票据保存成完整登录状态，错误提示会区分业务失败和网络失败。
- 歌单添加和删除统一优先使用现代 `AddSonglist` / `DelSonglist` 接口，传入数字歌曲 ID、歌单目录 ID 和歌单 ID；写入成功后重新读取目标歌单核对并刷新列表。
- 对请求结果不明确的超时先读取歌单确认，避免重复提交；旧接口只在具备网页凭证且现代接口明确失败时安全回退。

### 2. 平台歌单加载与刷新
- QQ 歌单直接复用歌单接口返回的歌曲详情，只为缺少详情的歌曲补查，减少完整歌单的逐首请求。
- 修复分页总数缺失、重复页导致的错误停止和循环，保留 MID、数字 ID、删除标识与歌曲顺序的对应关系。
- 各账号目录独立展示加载结果；一个账号响应慢或失败时不再阻塞其他账号，刷新和快速切换不会用旧响应覆盖当前列表。
- 刷新期间保留旧内容，失败显示提示；刷新按钮同时更新平台目录和当前歌单/每日推荐。

## 验证范围

- `npm run test:issue21`：17 项通过；相关 ESLint、主进程构建和渲染进程构建通过。
- macOS arm64 已实测 QQ 音乐二维码登录、账号目录加载、“我喜欢”歌单读取和刷新；QQ 添加/删除云端写入请使用本版本实际歌单继续验证。
- QQ 音乐微信扫码及 Windows/Linux 实机尚未在本轮完成云端验证，请反馈系统、登录方式和复现步骤。

## 建议与交流

仓库 [Discussions](https://github.com/xinzhihong-ship-it/lx-music-desktop-enhanced/discussions/18) 中已有微信群二维码，欢迎喜欢提建议、交流使用体验的朋友加入。请勿在群里发送账号 Cookie、token 等敏感信息；反馈问题时请附版本、系统和复现步骤。

---

# LX Music 增强版 3.2.1-beta.3

这是面向跨平台用户的测试版，重点验证实时歌曲基调识别（电音助手能力）、机架/DAW 后台毫秒级同步、QQ 音乐歌单添加修复（Issue #21）及多通道账号登录。请通过 GitHub Release 手动下载测试，不承诺 beta 自动更新。

## 新增功能与深度修复

### 1. 歌曲基调识别与机架实时同步（电音助手）
- **实时基调与调性轮盘展示**：播放条显示正在播放歌曲的调性（如 `1=C 大调`、`Am 小调`）与 Camelot 调性轮盘号（如 `8B`、`8A`）。
- **渐进式双阶音频分析**：首块音频解码后 1 秒内给出首段调性，后台静默完成全曲时间轴构建；相邻相同调性自动合并，低信噪比片段自适应整曲 K-S 算法兜底。
- **动态转调实时跟踪**：主歌、副歌、转调升降 Key 随播放进度毫秒级切换，弹窗与时间轴高亮跟随播放位置。
- **自定义修改与永久记忆**：支持整首调性设定或可视化分段微调，保存后永久记忆，下次播放瞬间加载无需重复计算；双击基调标签可一键强制重新分析。
- **独立置顶悬浮窗**：支持将基调面板独立脱离为主界面外的悬浮窗，自带 📌 窗口置顶、分段即时试听与主题样式自适应。
- **机架 / Auto-Tune 零配置同步**：内置 Antares Auto-Key 协议通道，自动下发调性至机架电音插件；支持电音深度 / Retune Speed（0~100）滑动调节与常用预设（0 强电音、20 流行、50 自然）。
- **后台与最小化全速同步**：主窗口与独立悬浮窗禁用 Chromium 后台节流（`backgroundThrottling: false`），采用音频时间更新事件流（`timeupdate`）与周期定时器双轨驱动。即使应用置于后台、最小化到托盘或被 Logic Pro / Cubase / Studio One / FL Studio 完全遮挡，依然保持毫秒级实时转调同步！

### 2. QQ 音乐歌单添加与平台账号系统修复（Issue #21）
- **修复加入新歌曲到 QQ 音乐歌单失败（Issue #21）**：
  - 修复 `toPlaylistMutationTrack` 字段映射与歌曲标识类型处理。
  - 修复 `fcg_music_add2songdir.fcg` 请求规范，采用标准 POST 表单方式与上下文参数组装，解决 `invalid request` 与 `no permit` 错误。
- **双步换票长效登录机制**：
  - 修复 QQ 音乐 App 扫码仅获取到临时凭证的问题，自动通过服务层二次换取具备长效生命周期与歌单写权限的完整 Session（包含长效 `musickey`、`p_lskey` 与刷新密钥）。
- **支持手机 QQ 音乐扫码与微信扫码双通道**：
  - 在登录弹窗提供原生卡片式二维码，免去任何第三方弹窗干扰。
- **大歌单并发崩溃修复**：
  - 针对大歌单（如 100+ 首歌曲）批量获取歌曲详情时引发的无限制并行请求问题，引入并发窗口控制（`concurrency: 6`），彻底杜绝 `Client network socket disconnected before secure TLS connection was established` 错误；单曲异常自动容错，不中断整单加载。

## 测试范围

- macOS arm64：已完成本机基调分析、时间轴转调、Auto-Key 同步、后台防节流与 QQ 歌单读写验证。
- Windows / macOS x64 / Linux：欢迎测试基调分析、悬浮窗置顶、机架同步与歌单操作。

---

# LX Music 增强版 3.2.1-beta.2

这是面向跨平台用户的测试版，重点验证 VST3 音频链、采样率显示、音频输出设备切换和 B 站视频播放。请通过 GitHub Release 手动下载测试，不承诺 beta 自动更新。

本次 beta 中，**macOS arm64 已完成本机验证**；Windows/Linux 及 ARM 目标由 CI 构建后仍需用户实机反馈，不把 CI 打包等同于实机验证。

## 本次修复

- 修复开启/关闭 VST3 或音频可视化时，输出设备绑定到旧 audio 元素或回到系统默认设备的问题。
- 统一音频路由重建和 `setSinkId` 的串行边界，快速切换设备时只保留最后一次有效选择。
- 设备枚举和路由绑定失败时不再把用户选择强制改写为 `default`。
- 路由重建失败时保留原有播放源、进度、音量和播放状态，不再因为临时设备错误替换成空元素。
- 增加 VST3 host 打包计划检查，交叉架构不会误复用旧的 host 二进制；host 缺失时显示明确错误。
- VST3 改为整条插件链共用一个宿主进程，限制过期音频积压，并提供可调桥接缓冲。
- 增加 WAV、FLAC、MP3、AAC、M4A、Ogg/Opus 等格式的轻量采样率识别；Opus 按实际播放输出显示 48 kHz，无法确认时显示未知。
- B 站音频探测复用 CDN 所需请求头；修复开发启动时 MPV 视频原生桥路径和构建竞态。
- beta 打包增加 lint、应用构建和 VST3 测试门禁，并保留跨平台 CI 检查。

## 测试范围

- macOS arm64：已完成本机 VST3 插件扫描、加载、参数、编辑器和输出设备矩阵验证。
- macOS x64：原生构建和 CI 验证，欢迎实机测试。
- Windows x64：原生构建、Rust/工作线程/路由测试通过，欢迎测试输出设备切换和插件编辑器。
- Linux x64：原生构建、Rust/工作线程/路由测试通过；X11 编辑器和实际音频后端欢迎测试。

## 已知限制

- VST3 仍是实验性功能，仅支持内置播放引擎；mpv 和 Audirvana 不支持 VST3 音频链路。
- Windows arm64、Linux arm64、Linux armv7l 的构建流程现在会携带对应架构的 VST3 host；这些目标仍需在真实 ARM 设备上验证插件兼容性。Windows 7 兼容包继续不携带 VST3 host。
- Windows 7 包仍属于兼容性测试产物，Electron、AudioWorklet、`setSinkId` 和原生插件编辑器尚未在真实 Windows 7 系统确认。
- Linux Wayland、Linux X11 原生插件窗口交互、ARM 真机、长时间播放、重型插件链和非零延迟插件仍需用户反馈。
- 第三方音源和在线音乐库的可用性取决于其自身服务状态；遇到音源失败时，请通过 GitHub Issues 提供脱敏日志和复现步骤。
- 发布包中的 beta 版本请手动下载安装；不要把本次版本当作正式稳定版。

发现问题时请附上系统版本、CPU 架构、播放引擎、是否开启 VST3/可视化、输出设备名称以及 `VST3` 设置页错误信息。
